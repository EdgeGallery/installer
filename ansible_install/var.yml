# Mode for online or offline install
NETWORK_MODE: offline

# Architecture amd64 or arm64
ARCH: amd64

# Absolute file path of source offline tarball file
TARBALL_FILE: /home/eg-all-amd64-latest-2021-01-25-12-16-55.tar.gz

# Dest path where offline tarball be decompossed into
TARBALL_PATH: /home/edgegallery-offline

# Whether clean the TARBALL_PATH before package
# true means clean and false means not clean
TARBALL_PATH_CLEANUP: true

# Could be true or false
# Currently only support false
ENABLE_PERSISTENCE: false

# Should set the following 2 params if ENABLE_PERSISTENCE is true
# NFS_SERVER_IP:
# NFS_PATH:

HELM_CHARTS_PATH: "{{ TARBALL_PATH }}/helm/helm-charts"
EG_IMAGE_TAG: latest
HELM_TAG: 1.0.0

APPSTORE_HELM_RELEASE_NAME: appstore-edgegallery
DEVELOPER_HELM_RELEASE_NAME: developer-edgegallery
MECM_FE_HELM_RELEASE_NAME: mecm-fe-edgegallery
ATP_HELM_RELEASE_NAME: atp-edgegallery

APPSTORE_PORT: 30091
DEVELOPER_PORT: 30092
MECM_PORT: 30093
ATP_PORT: 30094
USER_MGMT_PORT: 30067

# ip for portals, will be set to public IP of master node default or reset it here
#PORTAL_IP: 111.222.333.444
